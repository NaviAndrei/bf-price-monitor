## Summary

<!-- What does this PR do and why? -->

## Checklist

- [ ] Tests added/updated and passing (`pytest`)
- [ ] `requirements.txt` updated if dependencies changed
- [ ] Any new/changed GitHub Actions steps are pinned to a full commit SHA, not a floating tag
- [ ] No secrets committed; any new secret is documented and added via repo settings
- [ ] Acceptance criteria from the linked issue are met
- [ ] Docs updated (`README.md` / `CHANGELOG.md`) if user-facing behavior changed
